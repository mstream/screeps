{
  "name": "screeps",
  "description": "screeps bot",
  "scripts": {
    "lint-main": "eslint --fix -c eslint-main.json src/main",
    "lint-test": "eslint --fix -c eslint-test.json src/test",
    "pretest": "npm run lint-main && npm run lint-test",
    "test": "mocha --reporter spec 'src/test/*.js'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mstream/screeps.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.18.0",
    "lodash": "^3.10.1",
    "mocha": "^3.2.0",
    "nyc": "^10.1.2",
    "sinon": "^2.1.0"
  }
}
